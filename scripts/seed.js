require('dotenv').config();const bcrypt=require('bcryptjs');const {v4:uuid}=require('uuid');const db=require('../src/config/db');
(async()=>{const password=process.env.SEED_PASSWORD||'ChangeMe123!';for(const [email,role] of [['admin@reservecore.local','system_admin'],['manager@reservecore.local','facility_manager']]){const hash=await bcrypt.hash(password,12);await db.query(`INSERT INTO users(id,email,password_hash,role) VALUES($1,$2,$3,$4) ON CONFLICT(email) DO NOTHING`,[uuid(),email,hash,role]);console.log(`Seeded ${role}: ${email}`)}await db.end();})().catch(e=>{console.error(e);process.exit(1)});
