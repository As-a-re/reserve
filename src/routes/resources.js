const r=require('express').Router();const db=require('../config/db');const {authenticate,authorize}=require('../middleware/auth');r.get('/',authenticate,async(req,res,next)=>{try{const x=await db.query('SELECT * FROM resources WHERE active=true ORDER BY name');res.json({resources:x.rows});}catch(e){next(e)}});r.post('/',authenticate,authorize('system_admin','facility_manager'),async(req,res,next)=>{try{const {id,name,resourceType}=req.body;const x=await db.query('INSERT INTO resources(id,name,resource_type) VALUES($1,$2,$3) RETURNING *',[id||require('uuid').v4(),name,resourceType]);res.status(201).json({resource:x.rows[0]});}catch(e){next(e)}});module.exports=r;
